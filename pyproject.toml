[project]
name = "vpc-rca-agent"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "strands-agents>=0.1.0",
    "boto3>=1.42",
    "httpx>=0.28",
    "mcp>=1.0",
    "redis>=7.4",
    "pydantic-settings>=2.0",
]
