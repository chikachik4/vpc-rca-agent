# Branch Feedback

## main

- 상태: `codex`(CI/.gitignore) + `gemini`(RCA 모드 분기/JSON 보강) 변경이 반영된 기준 브랜치.
- 피드백:
1. 공통 기반(자동 검증 + 개발 산출물 제외)이 정리되어 협업 안정성이 상승했습니다.
2. 현재 CI는 `compile/import smoke` 중심이므로, 다음 단계로 `ruff`/`pytest`를 추가해 품질 게이트를 강화하는 것이 좋습니다.
3. GitHub 브랜치 보호 규칙에서 "필수 CI 통과 후 머지"를 설정하면 운영 품질이 더 안정됩니다.

## codex

- 상태: 기반 작업 브랜치. 핵심 변경은 `main`에 반영 완료되었습니다.
- 피드백:
1. 변경 범위가 작고 명확해 병합 리스크가 낮았습니다.
2. CI를 최소 구성으로 먼저 도입한 접근은 문제 파악/확장 측면에서 효율적입니다.
3. 후속으로 `lint`와 `test`를 별도 job으로 분리하고 캐시 최적화를 넣으면 실행 속도와 신뢰성이 개선됩니다.

## gemini

- 상태: RCA 기능 고도화 브랜치. 핵심 변경은 `main`에 병합 완료되었습니다.
- 피드백:
1. `beginner/expert` 모드 분기 로직은 유의미하며, Architect JSON 보정 로직도 실효성이 있습니다.
2. 후속 개선은 기능 분리 PR로 진행하는 것이 좋습니다.
   - 보안/TLS 및 HTTP 에러 핸들링 강화
   - MCP 라이프사이클 종료 처리 정교화
3. 각 PR에는 CI 통과 로그와 장애 재현 시나리오 결과를 첨부해 회귀를 방지하세요.

## 공통 권장 사항

1. 모든 브랜치에서 PR 템플릿에 "변경 목적/영향 범위/롤백 방법"을 고정 항목으로 추가합니다.
2. `main`에는 직접 push 대신 PR 머지만 허용합니다.
3. RCA 관련 변경은 read-only 원칙을 기본으로 하고, write 동작은 별도 승인 플로우에서만 허용합니다.
